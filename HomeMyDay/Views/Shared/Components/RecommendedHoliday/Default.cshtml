@model IEnumerable<HomeMyDay.Models.Holiday>

@if (Model.Count() > 0)
{
    <div class="holiday-feature">
        <div class="row">
            @foreach (var m in Model)
            {
                @Html.Partial("../../../Partials/RecommendedHolidayPreview.cshtml", m);
            }
        </div>
    </div>
}
@model IEnumerable<Accommodation>

@if (Model.Count() > 0)
{
    <div class="holiday-feature">
            @foreach (var m in Model)
            {
                @Html.Partial("~/Views/Shared/Partials/RecommendedAccommodationPreview.cshtml", m);
            }
        </div>
}